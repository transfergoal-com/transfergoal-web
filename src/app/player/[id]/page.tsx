import React from 'react';
import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Player page',
};

const PlayerByIdPage = () => {
    return <div>Player by Id Page</div>;
};

export default PlayerByIdPage;
